import styled from "styled-components";
import { SectionProducts as SectionP } from "../Perfumes/Perfumes.styled";

export const Main = styled.main`
    
`

export const SectionProducts = styled(SectionP)``

export const Img = styled.img`
    width: 100vw;
`